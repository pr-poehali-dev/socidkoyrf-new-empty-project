{
  "tests": [
    {
      "name": "Health check",
      "method": "GET",
      "path": "/?action=health",
      "expectedStatus": 200,
      "expectedBody": {"status": "ok"},
      "bodyMatcher": "partial"
    },
    {
      "name": "Память закрыта без входа",
      "method": "GET",
      "path": "/?action=all",
      "expectedStatus": 403,
      "expectedBody": {"error": "forbidden"},
      "bodyMatcher": "partial"
    },
    {
      "name": "Раздел памяти закрыт без входа",
      "method": "GET",
      "path": "/?action=section&name=done",
      "expectedStatus": 403,
      "expectedBody": {"error": "forbidden"},
      "bodyMatcher": "partial"
    }
  ]
}
